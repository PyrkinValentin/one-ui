import { color } from "../utils/convert"

export const lightFocusVars = {
	"--focus": color("#006FEE"),
}

export const darkFocusVars = {
	"--focus": color("#006FEE"),
}

export const focusColors = {
	focus: {
		DEFAULT: "rgb(var(--focus) / <alpha-value>)",
	}
}